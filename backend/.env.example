# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/cart-manager?retryWrites=true&w=majority

# Frontend URL (for CORS) - Add your production frontend URL here
CLIENT_URL=http://localhost:5173

# Production Example:
# CLIENT_URL=https://your-app.vercel.app

# Notes:
# - Replace 'username', 'password', and 'cluster' with your MongoDB Atlas credentials
# - For MongoDB Atlas, whitelist IP: 0.0.0.0/0 (allow all) for deployment
# - Don't commit the actual .env file to git
